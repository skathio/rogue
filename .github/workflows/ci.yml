# .github/workflows/ci.yml
name: ci
on:
  pull_request:
  push:
    branches: [main]

permissions: {}

jobs:
  ci:
    uses: skathio/hashira-ops/.github/workflows/nuget-package-ci.yml@70762f37277be0e7646953a7c2f16057e38ebbd5
    permissions:
      contents: read
      pull-requests: write
      security-events: write
    with:
      project_path: 'SkathIO.Rogue.sln'
      dotnet_version: '10.0.x'
      library_ref: '70762f37277be0e7646953a7c2f16057e38ebbd5'

  aot-publish:
    name: aot-publish
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "AOT publish — coming soon"

  bench-smoke:
    name: bench-smoke
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "Bench-smoke — coming soon"

  license-check:
    name: license-check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "License-check — coming soon"

  public-api:
    name: public-api
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "PublicApiAnalyzers — coming soon"
