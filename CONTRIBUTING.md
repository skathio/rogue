# Contributing to SkathIO.Rogue

Contributions are welcome. Please open an issue before starting large changes.

- Branch from `main`; target PRs at `main`
- `dotnet build SkathIO.Rogue.sln -c Release` must pass
- `dotnet test` must pass
- Follow the existing code style (`.editorconfig`)
