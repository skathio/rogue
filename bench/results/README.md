# Benchmark results

Benchmark results are committed after each baseline run.
See the CI `bench-smoke` job for the latest numbers.
Run `dotnet run -c Release --project bench/SkathIO.Rogue.Benchmarks -- --filter '*' --job short` locally.
